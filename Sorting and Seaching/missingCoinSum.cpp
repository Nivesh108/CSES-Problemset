#include<bits/stdc++.h>
#define         loop(n)     for(int i=0;i<n;i++)
#define         vi          vector<int>
#define         rep(a,b)    for(int i=a;i<b;i++)
#define         ll          long long
using namespace std;

int main(){
    
    int n;
    cin>>n;
    vi v(n);
    
    loop(n) cin>>v[i];
    sort(v.begin(),v.end());
    ll sum=v[0];
    
    rep(1,n){
        
    }

    return 0;
}