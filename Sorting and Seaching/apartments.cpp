#include<bits/stdc++.h>
#define         vi          vector<int>
#define         loop(n)     for(int i=0;i<n;i++)
#define         fast        ios::sync_with_stdio(0); cin.tie(0);
using namespace std;

int main(){

    fast
    int n,m,k,ans=0;
    vi v(n),w(m);
    loop(n) cin>>v[i];
    loop(m) cin>>w[i];
    sort(v.begin(),v.end());
    sort(w.begin(),w.end());
    

    return 0;
}